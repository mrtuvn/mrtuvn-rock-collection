import React from 'react';
import { BrowserRouter } from 'react-router-dom';
import { Routing } from './Routing';

const App = () => (
    <BrowserRouter>
        <Routing />
    </BrowserRouter>
);

export default App;